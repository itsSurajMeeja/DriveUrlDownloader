{
  "name": "driveurldownloader",
  "version": "1.0.0",
  "description": "The system is designed to download files from a list of specified URLs that are present in an xlsx file. If you have an xlsx file that contains a field of URL, the system will pick up that file and initiate the download process from Google Drive. Please note that the URL should be a Google Drive URL.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "surajmeeja",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.10.4"
  },
  "dependencies": {
    "node-xlsx": "^0.23.0"
  }
}
